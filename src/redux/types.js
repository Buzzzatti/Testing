import moduleName from "./module";

const prefix = `${moduleName}`;

export const LOADING = `${prefix}/LOADING`;
export const LOAD_PHOTOS = `${prefix}/LOAD_PHOTOS`;
export const LOADED_PHOTOS = `${prefix}/LOADED_PHOTOS`;
export const LOAD_USERS = `${prefix}/LOAD_USERS`;
export const LOADED_USERS = `${prefix}/LOADED_USERS`;
// export const LOAD_USER = `${prefix}/LOAD_USER`;
// export const LOADED_USER = `${prefix/LOADED_USER}`